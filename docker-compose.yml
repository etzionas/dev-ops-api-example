networks:
  fastapi-net:
    external: true

services:
  fastapi:
    image: tzionasev/fastapi:${IMAGE_TAG:-latest}
    container_name: fastapi-container
    restart: always
    ports:
      - "8000:80"
    networks:
      - fastapi-net